var NoteList = $(".content[data-brain-dump-id='<%= @note.brain_dump_id %>'").children('ul#brainDumpNotesList');
// var $HrLine = $(NoteList).closest('hr');
// if($HrLine.length < 1) {
//   $(NoteList).append('<hr>');
// }

$(NoteList).prepend("<%= j render partial: 'note', locals: { note: @note } %>");
$(".new_brain_dump_note")[0].reset();
$("textarea").val('');
